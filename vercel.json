{
  "buildCommand": "cd ALLOBRICOLAGE && npm install && npm run build",
  "outputDirectory": "ALLOBRICOLAGE/dist",
  "installCommand": "echo 'Skipping root install'",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
